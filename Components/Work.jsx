import SectionHeader from './sectionHeader';

const Work = () => {
    return(
        <section>
            <SectionHeader number={1}>Work I{'\''}ve Done</SectionHeader>

        </section>
    );
};

export default Work;
